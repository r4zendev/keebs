name: Build KLOR Wireless

on: [push, pull_request, workflow_dispatch]

jobs:
  left:
    uses: ./.github/workflows/base.build.yml
    with:
      keyboard: klor
      board: nice_nano/nrf52840/zmk
      shield: klor_left
  right:
    uses: ./.github/workflows/base.build.yml
    with:
      keyboard: klor
      board: nice_nano/nrf52840/zmk
      shield: klor_right
  merge:
    needs: [left, right]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/upload-artifact/merge@v4
        with:
          name: klor-wireless
          pattern: artifact-*
          delete-merged: true
