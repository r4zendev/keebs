name: Build ZMK firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  glove80:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: glove80/build.yaml
      config_path: glove80
      archive_name: glove80-firmware

  piantor:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: piantor/build.yaml
      config_path: piantor
      archive_name: piantor-firmware
