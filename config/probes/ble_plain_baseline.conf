# Probe firmware: remove the shared BLE extras and keep connection behavior boring.
#
# This is intentionally not a "recovery" build. It is an A/B baseline against
# the common config used by all wireless builds in this repo.
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=n

# Avoid split battery polling/proxy GATT work during the test.
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=n
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=n
CONFIG_ZMK_BATTERY_REPORTING=n

# Prefer more frequent link events over battery life for the repro test.
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=12
CONFIG_BT_PERIPHERAL_PREF_LATENCY=0
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=400

CONFIG_ZMK_SPLIT_BLE_PREF_INT=12
CONFIG_ZMK_SPLIT_BLE_PREF_LATENCY=0
CONFIG_ZMK_SPLIT_BLE_PREF_TIMEOUT=400

# Keep the controller/link negotiation surface small.
CONFIG_BT_DATA_LEN_UPDATE=n
