# nRF52 BLE stability profile.
#
# The internal LFRC costs a little more power than a healthy external LF crystal,
# but it avoids disconnects caused by marginal or faulty 32 kHz crystals on
# nRF52840 controller boards.
CONFIG_CLOCK_CONTROL_NRF_K32SRC_RC=y
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

